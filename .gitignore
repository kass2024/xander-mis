# --- User-requested paths ---
/admin/
/pdfs/
/PHPMailer/
/templates/
/uploads/
/vendor/

# Environment & secrets
.env
.env.*
!.env.example
parrot/
parrot/.env

# Apache
.htaccess
**/.htaccess

# Database credentials (not in repo — use db.example.php as template)
db.php
database.php
helpers/db.php

# Payment secrets live in .env — PHP files below are safe to commit (no keys in code)

# Logs & temp
*.log
logs/
error_log
**/error_log
*.tmp
*.bak
*.swp
.DS_Store
Thumbs.db

# IDE / OS
.idea/
.vscode/
.cursor/

# Credentials & keys (broad patterns)
*credentials*
*secrets*
*secret*.json
*service-account*.json
*.pem
*.p12
auth.json
config.local.php
local.php
**/stripe-keys*

# Google / Firebase service accounts & credential JSON
*.json
!composer.lock
parrotchatsupport*.json

# Temp & debug dumps
/temp/
/admission_letters/
error_log.txt
php_crash_log.txt
curl_debug_output.html
log.json

# Large / generated
node_modules/
*.zip
*.tar.gz
*.pdf
!docs/**/*.pdf
